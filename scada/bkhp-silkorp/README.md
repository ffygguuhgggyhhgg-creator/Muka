# БКХП «SilKorp» — проект Simple-Scada 2

Импорт присланного архива `BKHP SILKORP для доработки.rar` + машинно-читаемая
расшифровка бинарных файлов проекта.

**Проект:** `BKHP SILKORP 30.05.26 Актуальная`
**SCADA:** Simple-Scada 2 (формат `.spr/.svr/.spg/.swn/.ssc`)
**Отчёты:** Stimulsoft Reports (`.mrt`)

## С чего начать

| Документ | Содержимое |
|---|---|
| [`docs/01-описание-проекта.md`](docs/01-описание-проекта.md) | назначение, техсредства, состав объекта, соответствие заводских позиций индексам тегов |
| [`docs/02-схема-взаимосвязей.md`](docs/02-схема-взаимосвязей.md) | схема слоёв, цепочка управления механизмом, автомат задвижки, дерево скриптов, матрица доведённости |
| [`docs/03-алгоритмы.md`](docs/03-алгоритмы.md) | все 12 алгоритмов в псевдокоде с уставками |
| [`docs/04-таблица-переменных.md`](docs/04-таблица-переменных.md) | семейства тегов, дерево групп, соглашение по адресации ПЛК |
| [`docs/05-ошибки-и-исправления.md`](docs/05-ошибки-и-исправления.md) | **25 найденных дефектов**, исправления и методика проверки |
| [`fixes/README.md`](fixes/README.md) | как применить исправления и что проверить на объекте |

## Структура каталога

| Путь | Что это |
|------|---------|
| `BKHP_SILKORP_original.rar` | исходный архив без изменений (эталон для отката) |
| `project/` | распакованный проект Simple-Scada (бинарный, открывается в редакторе SCADA) |
| `decoded/scripts/*.pas` | 607 скриптов из `Scripts.ssc` в виде текста |
| `decoded/variables.tsv` | 1419 тегов: id, имя, тип, группа, привязка к OPC, описание |
| `decoded/scripts_tree.txt` | список скриптов с папками, числом строк и привязками |
| `decoded/messages.txt`, `decoded/project.txt`, `decoded/pages.txt`, `decoded/windows.txt` | строки из соответствующих файлов проекта |
| `docs/` | документация проекта (см. выше) |
| `docs/reference/` | машинные выкладки: `variables.md`, `io-map.md`, `scripts.md`, `tag-usage.tsv` |
| `fixes/scripts/` | 268 исправленных скриптов (обязательные правки) |
| `fixes/scripts-optional/` | 122 скрипта — правки по согласованию |
| `tools/` | парсеры бинарных форматов, анализаторы, генераторы |

`decoded/` — **только для чтения и поиска**: это точная расшифровка исходного
состояния. Правки вносятся в редакторе Simple-Scada; обратной сборки
`Scripts.ssc` не существует.

## Инструменты

| Скрипт | Назначение |
|---|---|
| `tools/parse_ssc.py` | разбор `Scripts.ssc` (607 скриптов, тип запуска, привязки; 99.7 % байт) |
| `tools/parse_svrr.py` | дерево тегов из `Variables.svrr` (100 % байт) |
| `tools/parse_svr.py` | привязки к OPC из `Variables.svr` |
| `tools/build_tables.py` | сводная таблица переменных |
| `tools/check_tags.py` | чтения/записи тегов, мёртвые теги, непривязанные входы/выходы |
| `tools/check_index.py` | несоответствия индексов в однотипных скриптах |
| `tools/check_selfclobber.py` | шаблон «условие проверяет флаг, изменённый этим же скриптом» |
| `tools/coverage.py` | матрица «тег → объект → обработчик → ПЛК» |
| `tools/apply_fixes.py` | генерация `fixes/` с проверкой шаблонов |
| `tools/gen_docs.py` | генерация `docs/reference/` |
| `tools/dump_simplescada.py` | универсальный дамп строк из любого файла проекта |

Все запускаются из каталога `scada/bkhp-silkorp` без аргументов.

## Инфраструктура (из `Project.spr`)

- **Архив:** PostgreSQL, БД `SlicorpBD`, `localhost`, пользователь `postgres`
- **OPC DA:** `TENSO_OPC_DA2.1` на `localhost` — весовой терминал Тензо-М
- **OPC UA:** `opc.tcp://192.168.40.200:4840` — ОВЕН ПЛК210 (Arm32, Linux, CODESYS V3),
  namespace `urn:plc200silkorp:Owen:PLC210...:OPCUA:Server`
- **Единицы:** Амперы, Гц (задание частоты ЧП), кг, ТОННЫ, %, секунды, счётчик
- Контроль связи с ПЛК — `StartPing('192.168.40.200', 1000, CheckBox2)`
  (см. `decoded/scripts/PingControl.pas`, `PingPLC16FlorStart.pas`)

## Состав автоматизации

Оборудование (по префиксам тегов):

| Префикс | Оборудование | Кол-во |
|---------|--------------|--------|
| `N`  | нории | 10 |
| `C`  | аспирация | 14 |
| `T`  | транспортёры (прямой/реверс) | 20 |
| `*Latch` | задвижки/клапаны: T1, T4, T4Clap, T5, T5Clap, T6, T7 | 7 групп × 8–10 |
| `T1..T7 Freq` | частотные преобразователи (задание частоты) | 7 |

Один экран `Управление` (`Page2`) + подстраница, окно `FreqWindow`
(«Задание частоты частотных преобразователей», поля Т1…Т7),
~455 текстовых и ~420 числовых полей, 60 бункеров (`Tank`), таблицы.

Отчёты: `VesReport` (за смену) и `VesReportInterval` (за интервал) —
колонки «Счётчик весов», «Счётчик оператора» и др., источник «Данные».

### Дерево скриптов (как в редакторе)

```
Скрипты Управления Механизмами
├── Кнопки блока аспирации / норий / транспортеров
├── Скрипты пуска механизмов
├── Скрипты остановки механизмов
├── Скрипты текста статусов механизмов
│   └── Нории / Аспирация / Транспортеры
├── Служебные скрипты
├── Скрипты отображения мнемосхемы
├── Скрипты аварийных блокировок
└── Контроль связи
Скрипты управления задвижками
├── Задвижки транспортера номер 6 / 7 / 4 / 5 / 1-3
│   ├── Кнопки управления
│   └── Алгоритмы управления
└── Скрипты отображения на мнемосхеме
Скрипты весового терминала
Скрипты работы с Частотными преобразователями
└── Транспортеры / 16 этаж
```

## Логика управления механизмом

`StatusN*/C*/T*` — состояние (`.AsInt`), используется в скриптах
`TextStatus*_OnDataChange`:

| Код | Состояние | Цвет |
|-----|-----------|------|
| 1 | В работе (А) — автоматический режим | зелёный |
| 2 | В работе (М) — местный режим | жёлтый |
| 3 | Стоп (М) | красный |
| 4 | Стоп (А) | красный |
| 10 | Авария | красный |

`StatusStart*` — команда пуска:

| Код | Значение |
|-----|----------|
| 5 | команда на включение выдана |
| 6 | отмена пуска |

Кнопка пуска (`StartButton*_OnClick`) проверяет неактивность выхода
контроллера (`RelayOut*.AsBool = False`) и отсутствие местного включения
(`Status* = 4`), затем выставляет `StatusStart* := 5` и поднимает
`ManualStartStatus` — таймер предупредительной сигнализации перед пуском.
Повторное нажатие при `StatusStart* = 5` отменяет пуск (`:= 6`).

Прочие семейства тегов на механизм: `RelayOut*`, `RelayIn*`,
`RelayStopIn*` (питание стоповой кнопки), `RelayAlarm*`, `AlarmStatus*`,
`StatusAlarm*`, `StartTime*`, `AlarmTime*`, `RPMTimerT*` (контроль оборотов).

Сообщения (`Messages.smg`) сгруппированы: «Пуск стоп нории / аспирация /
транспортеры», «Сообщения состояния механизмов», «Служебные сообщения».
На механизм — «Пуск», «Отмена пуска», «Стоп», «Отсутствие питания стоповой
кнопки».

## Как воспроизвести расшифровку

```bash
apt-get install -y unrar                       # 7-Zip 23.01 не берёт этот RAR5
unrar e -y BKHP_SILKORP_original.rar out/      # имена с кириллицей ломают -x
python3 tools/dump_simplescada.py out/Scripts.ssc > scripts.txt
```

Формат: последовательность строк `uint32 len` + `len` символов UTF-16LE
(в заголовках — `len` байт UTF-8). Скрипт хранится как имя обработчика,
служебный маркер и построчно текст на Pascal-подобном языке Simple-Scada.

## Состояние проекта

Проект находится в середине расширения: теги, мнемосхема, обработчики и
сообщения завёрнуты на полный состав оборудования (10 норий, 14 аспираций,
20 транспортёров), а привязка к ПЛК сделана только для 6 норий и
7 транспортёров; аспирация не привязана вовсе. Отсюда и название папки
«для доработки».

Первоочередное (подробно — в `docs/05-ошибки-и-исправления.md`):

1. 🔴 **D-01** аварийный останов включает аспирацию и оба направления
   транспортёров — исправлено в `fixes/`, применить в первую очередь.
2. 🔴 **D-02** 30 кнопок мнемосхемы не имеют обработчика: 11 механизмов
   не управляются с экрана. Правится переименованием в редакторе.
3. 🔴 **D-21** привязать к ПЛК аспирацию (42 тега) и транспортёры T8…T18.
4. 🟠 **D-04**, **D-03**, **D-05**, **D-19**, **D-22**, **D-16**, **D-08**.
